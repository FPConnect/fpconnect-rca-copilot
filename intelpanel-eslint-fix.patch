--- a/apps/web/src/components/IntelPanel.tsx
+++ b/apps/web/src/components/IntelPanel.tsx
@@
-    } catch (e) {
+    } catch {
       addNotification(
         "error",
         "Radar indisponível",
         "Não foi possível carregar o Radar. Verifique o backend (API).",
       );
